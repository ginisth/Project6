@model Project6.Models.Document

@{
    ViewBag.Title = "ViewDocument";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div>
    <h4>Document</h4>
    <hr />
    @if (Model.ProgressStatus == (int)Session["Role"])
    {
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Title)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Title)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Content)
            </dt>

            <dd>
                @Html.Raw(Model.Content)
            </dd>
        </dl>
        <p>
            @if (Model.ProgressStatus == 5)
            {
                @Html.ActionLink("Accept", "AcceptDocument", new { id = Model.Id }, new { @class = "btn btn-primary", @role = "button" })
                @Html.ActionLink("Decline", "DeclineDocument", new { id = Model.Id }, new { @class = "btn btn-primary", @role = "button" })
            }
        </p>
    }
    else
    {
        @Html.ValidationMessage("Documents", "Unregistered Action", new { @class = "text-danger" })
    }
</div>

